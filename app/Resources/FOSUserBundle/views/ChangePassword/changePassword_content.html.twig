{{ form_start(form) }}

{{ form_errors(form) }}

{{ form_row(form.current_password) }}
{{ form_row(form.new) }}

<input class="ui blue button" type="submit" value="{{ 'change_password.submit'|trans({}, 'FOSUserBundle') }}" />
{{ form_end(form) }}
